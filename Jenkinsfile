pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        echo 'unit test'
      }
    }
    stage('build') {
      steps {
        echo 'build'
      }
    }
    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }
  }
}